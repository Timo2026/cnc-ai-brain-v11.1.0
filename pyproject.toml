[project]
name = "cnc-ai-brain"
version = "11.0.3"
description = "CNC AI 工艺大脑 — 离线·串行多智能体·透明决策"
requires-python = ">=3.10"
readme = "README.md"

dependencies = [
    "gradio>=4.0",
    "pyyaml>=6.0",
    "loguru>=0.7",
    "requests>=2.31",
    "pydantic>=2.0",
]
